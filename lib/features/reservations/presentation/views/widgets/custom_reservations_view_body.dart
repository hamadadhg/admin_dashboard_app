import 'package:flutter/material.dart';

class CustomReservationsViewBody extends StatelessWidget {
  const CustomReservationsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
